server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:devdb;DB_CLOSE_DELAY=-1
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    com.suaempresa.seuprojeto: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG

jwt:
  secret: "652ca2150061c0de8f8e1927b996fafd"
  expiration: 86400000 # 24 horas em milissegundos